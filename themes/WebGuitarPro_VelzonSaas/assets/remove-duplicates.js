$( function()
{
	/**
	 * As Custom Module , Because Not Duplicated in All Pages
	 */
	$( '#tabCategoryFormParent' ).find( 'span:first' ).remove();
    $( '#tabFormCategoryTaxon' ).find( 'span:first' ).remove();
});