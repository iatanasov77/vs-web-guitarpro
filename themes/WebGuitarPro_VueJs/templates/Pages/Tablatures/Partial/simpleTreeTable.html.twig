{#  #}
<table class="table table-striped table-advance table-hover" id="tblTablatures">
	{#
	<thead>
        <tr>
            <th>{{ 'vs_cms.template.page_categories_index_row_category' | trans( {},'VSCmsBundle' ) }}</th>
            <th>{{ 'vs_cms.template.page_categories_index_row_actions' | trans( {},'VSCmsBundle' ) }}</th>
        </tr>
    </thead>
	#}
	<tbody>
        {% for item in userCategories %}
        	{% if not item.parent %}
        		{% include 'Pages/Tablatures/Partial/simpleTreeTableRows.html.twig' with {'category': item, 'parentId': 0, 'isTab': 0} %}
        	{% endif %}
        {% endfor %}
        
        {% include 'Pages/Tablatures/Partial/simpleTreeTableRowsUncategorized.html.twig' %}
    </tbody>
</table>


{# OLD TABLE
<table class="table table-hover">
    <thead>
        <tr>
            <th>Artist</th>
            <th>Song</th>
            <th>Play</th>
        </tr>
    </thead>
    <tbody>
{% for t in items %}
    {% if t.user == app.user %}
        <tr>
            <td>{{ t.artist }}</td>
            <td>{{ t.song }}</td>
            <td><a href="{{ path( 'vs_wgp_tablature_show', {id: t.id} ) }}" class="btn btn-primary">Play</a></td>
        </tr>
    {% endif %}
{% endfor %}
    </tbody>
</table>
#}
                