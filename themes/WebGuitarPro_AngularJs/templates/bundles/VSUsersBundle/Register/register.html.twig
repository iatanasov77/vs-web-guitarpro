{% extends 'base.html.twig' %} 

{% block content %}
	<!-- ============================================================== -->
    <!-- signup page  -->
    <!-- ============================================================== -->
    <div class="tablatures-container" style="margin-top: 110px">
        <div class="card card-registration-form">
            <div class="card-header">
                <h3 class="mb-1">{{ 'vs_users.template.register_header_title' | trans( {},'VSUsersBundle' ) }}</h3>
                <p>{{ 'vs_users.template.register_header_description' | trans( {},'VSUsersBundle' ) }}</p>
            </div>
            <div class="card-body">
               
                {{ form_start(form, { 'attr': {'class': 'form-horizontal', 'id': 'registration-form'} }) }}
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token( 'registration' ) }}" />
                    
                    {% if form_errors( form ) is not empty %}
                        <div class="alert alert-block alert-danger fade in">
                            <button type="button" class="close close-sm" data-bs-dismiss="alert">
                                <i class="icon-remove"></i>
                            </button>
                            <strong>Error!</strong> {{ form_errors( form ) }}
                        </div>
                    {% endif %}
                    
                    <div class="form-group">
                        {{ form_label( form.username, null, {'label_attr': {'class': 'col-form-label'}} ) }}
                        {{ form_widget( form.username, {'attr': {'class': 'form-control'}} ) }}
                    </div>
                    <div class="form-group">
                        {{ form_label( form.email, null, {'label_attr': {'class': 'col-form-label'}} ) }}
                        {{ form_widget( form.email, {'attr': {'class': 'form-control'}} ) }}
                    </div>
                    
                    <div class="form-group">
                        {{ form_label( form.prefered_locale, null, {'label_attr': {'class': 'col-form-label'}} ) }}
                        {{ form_widget( form.prefered_locale, {'attr': {'class': 'form-control'}} ) }}
                    </div>
                    
                    <div class="form-group mt-3">
                        {{ form_label( form.plain_password.first, null, {'label_attr': {'class': 'col-form-label'}} ) }}
                        <div class="input-group">
                            {{ form_widget( form.plain_password.first, {'attr': {'class': 'form-control'}} ) }}
                            <div class="input-group-append">
                                <button type="button" class="btn btn-primary" id="btnGeneratePasswordRegisterForm">{{ 'vs_users.form.generate_password' | trans( {},'VSUsersBundle' ) }}</button>
                            </div>
                        </div>
                    </div>
                    
                    <div class="form-group">
                        {{ form_label( form.plain_password.second, null, {'label_attr': {'class': 'col-form-label'}} ) }}
                        {{ form_widget( form.plain_password.second, {'attr': {'class': 'form-control'}} ) }}
                    </div>
                    
                    <div class="form-group mt-4">
                        <label class="custom-control custom-checkbox">
                            <input class="custom-control-input" type="checkbox" />
                            <span class="custom-control-label">
                                {{ 'vs_wgp.form.register.agreement_text' | trans( {},'WebGuitarPro' ) }} 
                                <a href="javascript:void(0);" data-bs-toggle="modal" data-bs-target="#terms-and-conditions-modal">
                                    {{ 'vs_wgp.form.register.terms_and_conditions' | trans( {},'WebGuitarPro' ) }}
                                </a>
                            </span>
                        </label>
                    </div>
                    
                    <div class="form-group pt-2 mt-4">
                        <button class="btn btn-block btn-primary" type="submit">{{ 'vs_wgp.form.register.button_register' | trans( {},'WebGuitarPro' ) }} </button>
                    </div>
                    
                {{ form_end(form, {'render_rest': false}) }}
            </div>
        </div>
    </div>
{% endblock %}

{% block widgets %}    
    {% include 'Modal/terms_and_conditions.html.twig' %}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    
    {{ encore_entry_link_tags( 'authentication', null, 'WebGuitarPro_AngularJs' ) }}
{% endblock %}  

{% block javascripts %}
    {{ parent() }}
    
    {{ encore_entry_script_tags( 'authentication', null, 'WebGuitarPro_AngularJs' ) }}
{% endblock %}
